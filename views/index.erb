<ul>

    <% @contacts.each do |contact| %>
      <li>
        <a href="<%= url("contacts/#{contact.id}") %>">
          <%= contact.name %></a>
          - <%= contact.phone_number %></li>
    <% end %>

</ul>
